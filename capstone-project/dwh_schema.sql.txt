-- This script was generated by a beta version of the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE softcart."softcartDimDate"
(
    date_id integer NOT NULL,
    date date,
    month integer,
    month_name character varying,
    day integer,
    weekday character varying,
    year integer,
    quarter integer,
    PRIMARY KEY (date_id)
);

CREATE TABLE softcart."softcartDimCategory"
(
    category_id integer NOT NULL,
    category character varying,
    PRIMARY KEY (category_id)
);

CREATE TABLE softcart."softcartDimItemfasd"
(
    item_id integer NOT NULL,
    item_name character varying,
    category_id integer,
    PRIMARY KEY (item_id)
);

CREATE TABLE softcart."softcartDimCountry"
(
    country_id integer NOT NULL,
    country_name character varying,
    PRIMARY KEY (country_id)
);

CREATE TABLE softcart."softcartFactSales"
(
    order_id integer NOT NULL,
    item_id integer,
    countery_id integer,
    date_id integer,
    price integer,
    PRIMARY KEY (order_id)
);

ALTER TABLE softcart."softcartFactSales"
    ADD FOREIGN KEY (item_id)
    REFERENCES softcart."softcartDimItemfasd" (item_id)
    NOT VALID;


ALTER TABLE softcart."softcartFactSales"
    ADD FOREIGN KEY (countery_id)
    REFERENCES softcart."softcartDimCountry" (country_id)
    NOT VALID;


ALTER TABLE softcart."softcartFactSales"
    ADD FOREIGN KEY (date_id)
    REFERENCES softcart."softcartDimDate" (date_id)
    NOT VALID;


ALTER TABLE softcart."softcartDimItemfasd"
    ADD FOREIGN KEY (category_id)
    REFERENCES softcart."softcartDimCategory" (category_id)
    NOT VALID;

END;